#include <stdio.h>

int main() {
  int n,sum=0,r,contri=1;
  printf("enter the binary number\n");
  scanf("%d",&n);
  while(n!=0)
  {
      r=n%10;
      sum=sum+(r*contri);
      n=n/10;
      contri=contri*2;
  }
  printf("decimal form is\n %d",sum);
    return 0;
}
