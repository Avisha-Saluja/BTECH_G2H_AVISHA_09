#include <stdio.h>

int main() {
   int a,b,i,j;
   int sum=0;
   printf("enter size of row and coloumn");
   scanf("%d%d",&a,&b);
   int ar[a][b];
   int aar[a][b];
   printf("enter elements");
   for(i=0;i<a;i++)
   {
       for(j=0;j<b;j++)
       {
           scanf("%d",&ar[i][j]);
       }
   }
   printf("enter elements");
   for(i=0;i<a;i++)
   {
       for(j=0;j<b;j++)
       {
           scanf("%d",&aar[i][j]);
       }
   }
   for(i=0;i<a;i++)
   {
       for(j=0;j<b;j++)
       {
          sum=sum+ar[i][j]+aar[i][j];
       }
   }
   printf("sum is \n %d",sum);
   
 return 0;
 
}
