#include <stdio.h>

int main() {
   int a,b,i,j;
   printf("enter size of row and coloumn");
   scanf("%d%d",&a,&b);
   int ar[a][b];
   printf("enter elements");
   for(i=0;i<a;i++)
   {
       for(j=0;j<b;j++)
       {
           scanf("%d",&ar[i][j]);
       }
   }
   printf("origninal is \n");
   for(i=0;i<a;i++)
   {
       for(j=0;j<b;j++)
       {
          printf("%d ",ar[i][j]);
       }
       printf ("\n");
   }
  printf("transpose is \n");
   for(i=0;i<a;i++)
   {
       for(j=0;j<b;j++)
       {
          printf("%d ",ar[j][i]);
       }
       printf ("\n");
   }
   
 return 0;
 
}
